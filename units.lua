function UnitsCreate()
    return {
        unitTypes = {},
        units = {},
        inif = function(self)
            -- Create units
        end,
        draw = function(self, grid)

        end
    }
end

return UnitsCreate()
